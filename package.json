{
	"name": "bidijkstra",
	"version": "0.6.3",
	"description": "bi-directional dijkstra search",
	"main": "index.js",
	"scripts": {
		"build": "tsc",
		"lint": "eslint **/*.js **/*.ts --ignore-path .gitignore",
		"pretest": "npm install",
		"preversion": "npm run test",
		"version": "",
		"postversion": "git push && git push --tags && npm publish",
		"test": "npm run lint && node --test ./tests/*.spec.js",
		"start": "node examples.js"
	},
	"Author": "john@invisiblearchitects.com",
	"license": "MIT",
	"devDependencies": {
		"eslint": "^5.16.0",
		"nodemon": "^1.19.4",
		"typescript": "^4.0.0",
		"@types/node": "^14.0.0",
		"ts-node": "^9.0.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/invisiblegaudi/bidijkstra"
	},
	"eslintIgnore": [
		"./node_modules/*"
	],
  "publishConfig": { "registry": "https://npm.pkg.github.com/" }
}
